# GoMaster v0.8 功能规划：从「能讲」到「讲得准 · 用得顺 · 带得走」

> 规划日期：2026-08-14　|　基于当前稳定版 v0.7.7（Electron 桌面 exe 已落地）
> 目标：把程序从"跑通链路"升级为"教学可用、配置自决、产出可带走"

---

## 0. 版本定位与总览

| 维度 | v0.7 现状 | v0.8 目标 |
|------|-----------|-----------|
| 讲解质量 | 通顺但偏口语化、偶有编造棋理 | **准确、可验证、引用真实棋理** |
| 知识来源 | 纯靠大模型记忆 | **内置围棋知识库 + RAG 检索增强** |
| 产出物 | 仅 Markdown 文字报告 | **含棋盘图例的 PDF 报告** |
| 配置 | 路径硬编码 + Key 仅 .env，无 UI | **图形设置界面 + 本地配置文件** |
| 模型 | 仅 DeepSeek 硬编码 | **可自定义任意 OpenAI 协议大模型** |

六大方向 → 模块映射：

```
方向1 解读更准确  ──► src/explainer.py（Prompt 重构 + 结构化输出 + 降温）
方向2 围棋知识库  ──► knowledge/（小巴联网建库，结构化条目 + 索引）
方向3 用好知识库  ──► src/rag.py（新建 retrieve() 接入现有钩子）
方向4 PDF导出    ──► 报告视图 + Electron printToPDF + 后端 /api/report
方向5 设置界面    ──► 设置页 UI + config.json 本地存储 + /api/settings
方向6 其他增强    ──► 胜率曲线 / 19路 / 变化树 / 缓存 / 离线模型…（精选落地）
```

---

## 1. 方向1：DeepSeek 解读更准确（而非更口语化）

### 问题诊断（来自 explainer.py 现状）
- **软硬要求打架**：system 同时要求"具体棋理 + 局部棋形"与"语气鼓励、易懂、生活化比喻"——后者诱导模型优先追求通顺而非精确。
- **temperature=0.25 偏发散**，容易生成"打个比方"式空话替代实质分析。
- **无结构化校验**：模型可能编造棋理/棋谚，且无"不确定就明说"的兜底约束。
- **无知识锚定**：只能凭训练记忆，是幻觉与口语化的共同根因（方向2/3 是根本杠杆）。

### 改造方案
1. **Prompt 重构——把"准确性"设为第一约束**
   - 删除"生活化比喻""鼓励"等软性指令；改为"**精确、可验证、使用规范棋术语（定式/棋形/手筋/厚薄/势力/眼位/官子）**"。
   - 显式禁令：`禁止杜撰定式或棋谚；若引用，必须来自知识库或公认棋理并标注出处；不确定时明确说"此处建议摆谱验证"，不得含糊带过。`
   - 保留：坐标铁律（GTP 记号）、基于真实局面（非变化图）讲解。
2. **结构化输出（JSON 字段约束）**
   - 要求模型输出固定结构：
     ```
     {
       "问题定性": "子力重复/方向偏差/忽视弱棋/被抢要点/死活误判/官子损目…",
       "关键棋理": ["定式名/棋形名/棋谚", …],
       "推荐点意图": "拆边/挂角/守角/打入/做活/出头/收官…",
       "后续2-3手推演": "对方大致应法 + 预期形势",
       "不确定点": "若有存疑，在此说明"
     }
     ```
   - 好处：前端可分区展示、便于自动校验"引用的棋理是否真实存在"。
3. **降温 + 自检**：`temperature` 0.25 → **0.1**；输出前自检坐标格式与棋理一致性（复用现有 `_has_bad_coords`）。
4. **（可选）两遍法**：第一遍只从 KataGo 数据抽取客观事实（不编造），第二遍生成讲解，事实层与数据严格对齐。
5. **RAG 注入**（见方向3）：把可信棋理作为【参考资料】前置，从根本上替代"凭记忆编"。

### 验收标准
- 抽 10 个失误手复核：①坐标 100% 正确；②引用的定式/棋谚可溯源（知识库或公认）；③不再出现"好比…""就像…"式空泛比喻；④问题定性落在预设分类之一。

---

## 2. 方向2：围棋知识库（小巴负责联网检索 + 建库）

> 明确分工：**由我（小巴）利用联网检索能力建立知识库，并纳入程序 `knowledge/` 目录**，随版本持续扩充。

### 知识库结构（`knowledge/`）
- 分类（category）：
  - `joseki` 定式（星/小目/高目/目外/三三/大斜/妖刀/雪崩…）
  - `shape` 棋形（好形/恶形、梅花六、斗笠形、裂形…）
  - `tesuji` 手筋（挖/夹/渡/扑/枷/倒扑/滚打包收…）
  - `life_death` 死活（常型、做眼、点杀、有眼杀无眼…）
  - `endgame` 官子（收官次序、目数计算、扳粘…）
  - `fuseki` 布局（占角/守角/挂角/拆边/模样/侵消…）
  - `proverb` 棋谚（敌之要点我之要点 / 金角银边草肚皮 / 左右同形走中央…）
  - `pattern` 实战常型（打入、治孤、对杀、征子…）
  - `game` 名局片段（职业棋手典型处理，可选）
- 单条 entry 字段：
  ```json
  {
    "id": "joseki-star-keima",
    "category": "joseki",
    "title": "星位小飞挂·一间跳定式",
    "content": "讲解文本（含为何两分、后续要点）",
    "tags": ["星位", "小飞挂", "一间跳", "两分"],
    "board_features": ["角部", "星位", "挂角"],   // 用于检索匹配
    "difficulty": "18K-5K",
    "source": "https://...",
    "diagram": "可选 ASCII/SGF 局面"
  }
  ```
- 机器可读索引：`knowledge/index.json`（全部 entry 摘要），供 `rag.py` 加载。

### 我检索的来源范围
- **Sensei's Library**（senseis.xmp.net）：英文权威围棋百科，定式/手筋/死活体系完整。
- **101 围棋网**题解与定式区、百度百科定式词条、公开《定式大全》摘要。
- **经典死活/棋形**：死活辞典（上卷）实战常型、发阳论/玄玄棋经选段、围棋宝典常型。
- **棋谚汇总**：整理 30~50 条高频棋谚并标注适用局面。
- 注：所有内容**经我二次校验**，避免错误棋理污染讲解（这是知识库最大的风险点）。

### v1 建库目标
- 首批 **80~120 条高质量条目**，优先覆盖初学者最易踩的：星/小目基础定式、10 种常见棋形、15 种基础手筋、死活常型、官子次序、20+ 棋谚。
- 入库格式：每条一个 Markdown + 汇总 `index.json`。
- 增量更新：后续我持续补充（定式变型、AI 新定式、更多死活），版本迭代时纳入。

---

## 3. 方向3：让程序更好用知识库（RAG 接入）

### 关键发现
`src/explainer.py` 第 21-24 行**已预留钩子**：
```python
try:
    from rag import retrieve as _rag_retrieve
except Exception:
    _rag_retrieve = None
```
且第 147-158 行已有"若 `_rag_retrieve` 存在就检索并拼成【参考资料】"的逻辑。**只需新建 `src/rag.py` 实现 `retrieve()` 即可点亮，无需改 explainer 主流程。**

### 实现设计
1. **`src/rag.py` 的 `retrieve(query, top_k=3)`**
   - 轻量 RAG，**零依赖优先**：先按 `category` + `tags` 关键词过滤，再按语义相关度排序取 top-k。
   - 语义排序方案（分级）：
     - **在线优先**：复用设置里的大模型 embedding 接口（OpenAI 协议 `/embeddings`），把 query 与 entry 向量化后余弦排序。
     - **离线兜底**：纯 `board_features` + 关键词匹配（BM25 思路），无网也能命中"星位挂角""征子"等常型——虽不如向量精准，但足够降低幻觉。
2. **Query 增强（核心）**：不只用"黑方第N手 实际X 推荐Y 阶段"，还从局面**提取概念标签**：
   - 角部争夺 / 边上拆 / 打入 / 侵消 / 征子 / 做活 / 杀棋 / 对杀 / 收官 / 治孤…
   - 用标签去匹配 `board_features`，命中对应知识条目（比纯文本语义更准）。
3. **注入与引用标注**：作为【参考资料】喂给讲解器（复用现有拼接）；讲解末尾自动标注"参考：定式·星位小飞挂"，增强可信与可追溯。

### 效果
讲解从"闭卷凭记忆"变为"开卷有依据"——**准确性↑、幻觉↓、口语化↓**，与方向1 协同。

---

## 4. 方向4：PDF 导出（含问题手图例）

### 现状
`_build_report()` 仅生成 Markdown（文字 + 表格），无图，初学者"看不见哪步差在哪"。

### 方案（Electron 优先，最稳）
1. **新增「复盘报告视图」（React 页）**
   - 每个失误手一张卡片：**左侧 Canvas 棋盘图**（落子前局面 + ◆实际落子 / ★AI 推荐 + 推荐后续蓝色虚线）+ **右侧讲解 + 胜率差 + 棋理标签**。
   - 棋盘重绘复用现有 `Board.tsx` 的 Canvas 逻辑，保证与 App 内显示一致。
2. **一键导出 PDF**：用 Electron `webContents.printToPDF()`，保留矢量棋盘 + 文字，清晰度高。
3. **后端补 `GET /api/report/<id>`**：返回每手完整局面（`moves` 序列 + 失误手标记 + 讲解），前端据此重绘各失误手棋盘图。
4. **纯 web 模式兜底**（次要）：导出**内联 SVG 棋盘的 HTML**，用户浏览器"打印→另存 PDF"。保证无 Electron 也能带走图。

### 验收标准
导出 PDF 含：①封面（对局信息：黑白名、贴目、手数、分析参数）；②每个失误手的**棋盘图 + 讲解 + 胜率差**；③（可选）全局胜率曲线（SVG 折线，见方向6）。

---

## 5. 方向5：设置界面（本地路径 + 自定义大模型）

### 现状
路径硬编码在 `src/config.py`；API Key 仅供 `.env`；无任何 UI；Mac/Windows 路径不同易踩坑。

### 实现
1. **设置项清单**
   | 分组 | 字段 |
   |------|------|
   | 引擎 | 本地 KataGo 程序位置（exe/可执行）、KataGo 配置文件（analysis_example.cfg）位置、权重文件位置 |
   | 输出 | 分析/复盘结果输出目录 |
   | 大模型 | API Base URL、API Key、Model Name（默认 DeepSeek，兼容 OpenAI / Ollama / 任意 OpenAI 协议） |
   | 分析参数 | maxVisits、失误阈值 threshold、用户水平（入门/进阶/挑战，已有） |
2. **本地存储**：`config.json` 存于用户目录（Electron 用 `app.getPath('userData')`；纯 web 用 LocalStorage 兜底）。**不入库、不泄漏敏感信息**。
3. **后端读取**：`config.py` 增加 `load_settings()` 覆盖默认值；`explainer.py` / `rag.py` 读自定义 LLM 配置（Base/Key/Model）。
4. **接口与 UI**：`GET/POST /api/settings`；React 设置页（表单 + 文件浏览按钮 + **"测试 KataGo"/"测试 API"连接校验**）。
5. **首次启动向导**（降门槛）：自动探测 KataGo、校验权重、测试 API，引导填写缺失项。

### 验收标准
在设置页改 KataGo 路径 / 换用自建大模型（如 Ollama 本地模型），不重打包即生效；配置存本地、重开程序保留。

---

## 6. 方向6：我推演的其他可改进方向（精选）

| # | 方向 | 价值 | 工作量 |
|---|------|------|--------|
| 1 | **全局胜率曲线**（SVG 折线，标最大失误点） | 一眼看全局走势，定位崩盘点 | 小 |
| 2 | **9/13/19 路全尺寸支持**（权重+perf 调优） | 当前仅 9 路实测，19 路才是主战场 | 中 |
| 3 | **变化树可视化 + 分支切换**（现仅线性 PV） | 多步变化初学者难还原 | 中 |
| 4 | **假设推演**（点棋盘任意点→即时分析"如果下这里"） | 主动学习，不止被动复盘 | 中 |
| 5 | **复盘会话保存 / 历史库**（SGF + 讲解二次打开） | 跨次对比、长期追踪进步 | 小 |
| 6 | **SGF 导出带教练评语**（外部工具可看） | 与 101/弈客等生态互通 | 小 |
| 7 | **失误分类统计 + 个性训练建议**（"你70%失误在官子"） | 从"看报告"到"知道练什么" | 中 |
| 8 | **离线/本地大模型（Ollama）支持** | 隐私 + 0 出网费用，呼应方向5 | 小 |
| 9 | **语音播报**（浏览器 SpeechSynthesis 免费离线 / TTS API） | 通勤/用眼疲劳场景 | 小 |
| 10 | **分析中缓存**（同局面不重复算） | 大棋谱提速，已列 TODO | 小 |
| 11 | **知识库浏览器**（程序内查阅定式/手筋） | 把 KB 变成可学的"围棋小百科" | 中 |
| 12 | **明暗主题 / 重点手收藏笔记** | 体验细节 | 小 |

---

## 7. 落地路线图（建议顺序）

**Phase A —— 准确性三件套（最高性价比，先打）**
- 方向1（explainer.py Prompt 重构 + 结构化 + 降温）
- 方向2（小巴建库 v1：80~120 条 + index.json）
- 方向3（新建 src/rag.py 接入现有钩子）
- 改动文件：`src/explainer.py`、`src/rag.py`（新）、`knowledge/*`
- 验证：抽 10 手复核准确性（见方向1 验收）

**Phase B —— 可用性（配置自决）**
- 方向5（设置页 + config.json + /api/settings + 首次向导）
- 改动文件：`frontend/src/components/Settings.tsx`（新）、`server.py`、`src/config.py`、`electron/main.cjs`
- 验证：换 KataGo 路径 / 换大模型即生效，配置持久化

**Phase C —— 产出物（带走图）**
- 方向4（报告视图 + printToPDF + /api/report）
- 改动文件：`frontend/src/components/ReportView.tsx`（新）、`server.py`、`electron/main.cjs`
- 验证：导出 PDF 含棋盘图例

**Phase D —— 增强（精选 2~3 个）**
- 建议优先：①胜率曲线 ②19 路支持 ③分析中缓存
- 其余按需排期

---

## 8. 风险与依赖
- **Embedding 选型**：在线（需 API）vs 离线（纯关键词）。建议默认离线兜底 + 设置里可选 embedding API，零网也能用。
- **知识库质量**：错误棋理比没有更糟——所有条目经小巴二次校验，并标注 `source` 便于追溯。
- **PDF 在纯 web 模式**：Electron `printToPDF` 不可用，退化为"导出含 SVG 的 HTML 自行打印"。
- **本地大模型配置**：Ollama 等需用户在设置里填 Base URL，提供"连接测试"降低门槛。
- **19 路性能**：CPU 下 maxVisits 需更低或引入 GPU/云端 KataGo（方向5 预留自定义引擎入口）。

---

## 9. 决策与进度（老马 2026-08-15 拍板）

- **版本号**：定为 **v0.8**（server.py /api/version 与 package.json 已 bump 到 0.8.0）。
- **落地顺序**：先开 **Phase A（准确性三件套）**。
- **知识库**：**均衡**铺 8 类，v1 已建 85 条。

### Phase A 完成状态（2026-08-15）
| 任务 | 交付 | 状态 |
|------|------|------|
| 方向2 知识库 v1 | `knowledge/build_kb.py` + 生成的 `knowledge/index.json`(85条) + `knowledge/kb.md` | ✅ |
| 方向3 RAG 接入 | `src/rag.py`（离线双字重叠+结构化加权，预留 embedding 钩子；explainer 已有钩子直接点亮） | ✅ |
| 方向1 提示词重构 | `src/explainer.py`：删口语化要求、加"禁止杜撰/不确定明说"硬约束、5 段结构化输出、temp 0.25→0.1、RAG query 带 phase+zone | ✅ |
| 版本 bump | server.py 0.7.7→0.8.0；package.json 0.7.7→0.8.0（打包目录 dist-electron-v080） | ✅ |
| 离线自测 | RAG 检索命中（布局→定式/中盘→死活/官子→官子）；explainer 真实路径+RAG 注入不崩、5 段结构正确 | ✅ |

### 下一步（待老马确认）
- Phase A 代码尚未推 GitHub（改动涉及 src/explainer.py、src/rag.py、knowledge/*、server.py、package.json）。
- 真机验证：老马本机跑一局复盘，看讲解是否明显更"准"（可对比 v0.7.7）。
- 之后按路线图推进 **Phase B（设置界面）/ C（PDF 导出）/ D（胜率曲线·19 路·缓存）**。
