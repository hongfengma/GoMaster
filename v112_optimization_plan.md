# GoMaster v1.1.2 整体优化方案

> 针对老马 8/15 晚间反馈的三个典型错误：
> 1. R14（第三线）被 LLM 描述成「第四线」；
> 2. 星位定式挂角名错误（如把「小飞/一间高挂」识别成「二间低挂」）；
> 3. Q13 与 Q4 被错误关联为「拆二」、S14 被错误解读为「压迫白棋右边」等方向感错误。
>
> 老马明确要求：**不要只针对一两个点修，要做整体优化**。因此先输出本方案，明日执行。

---

## 一、根因分析

三个问题表面不同，但根因相通：**程序没有给 LLM 提供足够精确、可验证的“空间事实”，LLM 只能凭 ASCII 棋盘或自身空间直觉瞎猜，导致线的计数、定式命名、远近关联、方向压迫等幻觉**。

| 问题现象 | 直接根因 | 深层根因 |
|---|---|---|
| R14 被说成「第四线」 | `zone_of_gtp` 只输出「边上」，没给出「第几线」 | LLM 自己数线，方向/计数易错；围棋“第 N 线”没有显式锚定 |
| 星位挂角名错误 | `_detect_joseki` 取“角部区域内第一个异色子”作为挂角方 | 没区分“挂角方”与“后续应手”，把任意落在角部半径内的子都拿来命名 |
| P5/Q13 被错误关联 | 事实单只针对实际落子 P5，推荐点 Q13 无事实约束 | LLM 对推荐点自由发挥，跨半盘硬凑“拆二” |
| S14「压迫右边」 | 阶段/方向判断失真 | 阶段判定把部分厚势误判为战斗；LLM 没拿到推荐点的区域/远近事实 |

---

## 二、整体优化方向

### 2.1 坐标与“第 N 线”标准化

围棋中「第 N 线」必须按**最近边向里数、1-based**：
- 距边 0 格 = 第一线，1 格 = 第二线，2 格 = 第三线，3 格 = 第四线，≥4 格 = 中腹（第五线及以上）。

修改：
- `go_board.py` 新增 `line_of_xy(x, y, size)`：返回 `(最近边方向, 第几线)`，例如 R14 → `('右边', 3)`。
- `go_board.py` 新增 `line_of_gtp(gtp, size)` 包装。
- `fact_to_text` 在每条事实里明确输出：
  - `本手 {actual} 位于「右边第三线」，区域「边上」。`
  - `推荐点 {best} 位于「右边第三线」。`
- 区域判定同步细化：第四线及以下为「边上/角部」，第五线及以上才为「中腹」；一二线必须称「边线/底线」。

### 2.2 定式识别重构

当前 `_detect_joseki` 的问题：
1. 取区域内第一个异色子当挂角方，顺序依赖强；
2. 对当前手（黑 R14）仍套首子定式名，角色混淆；
3. `(dx,dy)` 映射表已改过一次，但符号/方向仍可能因角部不同而错乱。

重构方案：
- **只识别“首子 + 对方挂角 + 应手”这一完整序列**，当前手必须是其中一员才给名。
- 挂角方判定：在角部首子周围 5 线内，找**最近**的异色子作为挂角方（不是按落子顺序第一个）。
- 当前手角色：
  - 若当前手就是首子 → 输出角部着法名（星位/小目/三三…）；
  - 若当前手是挂角方 → 输出挂角关系名；
  - 若当前手是应手方 → 输出“某定式·某手的应手”；
  - 若当前手只是落在角部附近、不在定式序列中 → 输出“定式外/脱先/大场”，不再硬套定式名。
- 校正 `(dx,dy)` 到中文术语的映射，覆盖四个角对称方向：

| 相对首子偏移 (dx,dy) | 术语 |
|---|---|
| (1,1) | 小尖挂 |
| (1,2) / (2,1) | 小飞挂 |
| (1,3) / (3,1) | 一间高挂 |
| (2,0) / (0,2) | 二间低挂 |
| (2,2) | 二间高挂 |
| (2,3) / (3,2) | 大飞挂 |
| (1,0) / (0,1) | 一间低挂 |
| (3,0) / (0,3) | 三间低挂 |

- 当识别置信度低（如首子不是标准角部着法、挂角方距离过远、当前手不在应手集合内）时，**不输出定式标签**，避免误导 LLM。

### 2.3 推荐点也生成事实单

当前只给 `actual` 生成事实，LLM 对 `best` 的描述只能自由发挥，导致：
- “Q13 与 Q4 形成拆二”这种跨半盘错误。

修改：
- `extract_fact` 拆成 `extract_fact(...)` 返回 `actual_fact` 与 `best_fact`。
- `best_fact` 至少包含：zone、line、附近的 shape、定式上下文、connection（边线提示）。
- `fact_to_text` 增加「推荐点事实」段落。
- 在 LLM prompt 中加入：**描述推荐点时只能引用推荐点事实单内的坐标与棋形；禁止把推荐点与 3 格以外的子强行关联为“拆二/配合”**。

### 2.4 拆二 / 联络 / 方向判断严格化

- **拆二**：当前手落子后，必须与某个同色子在正交方向间隔恰好 1 格、中间为空，且两子在同一线或相邻线（距离 ≤ 2）。
- **双（对角）**：对角相邻且均为同色。
- **联络/补断**：用落子前棋盘判断；若落子前（x,y）正交相邻已有 ≥2 块己方棋群，则本手为联络/补断。
- **方向判断**：
  - 实际点与推荐点分别位于不同区域（角/边/中腹）→ 方向偏差；
  - 实际点远离己方现有子力、靠近对方厚势 → 方向偏差；
  - 用“最近边方向 + 线数”替代模糊方位词。

### 2.5 阶段判断改进

当前 `unsettled` 阈值 = 2，但 19 路棋盘上很多厚势/活棋气也很少，会被误判为战斗。

修改：
- 阈值从 2 放宽到 3（避免厚势被误判），但增加**棋子群大小过滤**：只把 ≥3 子且气数 ≤3 的群算“未安定大龙”。
- 如果 KataGo 返回 ownership，用 ownership 均值过滤：某棋子群所在区域己方 ownership > 0.7 → 视为安定，不计入 unsettled。
- 空点比例阈值按棋盘大小动态：19 路 `empty_ratio < 0.35` 且 unsettled==0 为官子；>0.55 为布局；其余为中盘。

### 2.6 LLM Prompt 强化

- 在事实单里加入**禁止远距离关联**铁律：
  > “讲解中描述某点与周边子的关系时，只能引用距该点 3 格以内的棋子；禁止把 Q13 与远在对角的 Q4 说成拆二或配合。”
- 明确**推荐点事实单**的使用规则：
  > “【推荐点事实】是程序对 AI 推荐点的确定性计算。你描述推荐点意图时，必须基于以下事实，不得自行构造跨盘关系。”
- 对“压迫/扩张/联络”等动词加约束：必须有事实单中的 `connection`、`nearby` 或 `line` 支持。

### 2.7 误报降级机制

- 给事实标签增加 `confidence`（高/中/低），前端只对 `confidence == 高` 的标签做棋盘标注；低置信度标签不进入 LLM prompt。
- 当定式/棋形判断不确定时，标签文本写成“疑似 X”，不写入 `fact_tags`，避免红色/绿色标签误导。

---

## 三、明日执行清单（v1.1.2）

1. **go_board.py**
   - 新增 `line_of_xy` / `line_of_gtp` / `nearest_edge`。
   - 调整 `zone_of_xy`：第四线及以下为边/角，≥第五线为中腹。

2. **fact_extractor.py**
   - 重写 `_detect_joseki`：按“首子—挂角—应手”角色识别，输出 `confidence`。
   - 新增 `extract_fact` 返回 `best_fact`；`fact_to_text` 输出 actual + best 双事实单。
   - 调整 `_detect_shapes` 的拆二/双判定距离上限。
   - 调整 `_detect_connection` 用落子前棋盘判断联络。
   - 调整 `detect_phase` 阈值与 ownership 过滤。

3. **explainer.py**
   - Prompt 中加入推荐点事实单、禁止远距离关联、线的精确描述等约束。
   - Verifier 增加对“跨盘关联”的检测。

4. **review.py / types / 前端**
   - `ReviewEntry` 增加 `best_fact` 字段。
   - 前端 `MistakeList` 与 `Board` 标签按 confidence 过滤。

5. **测试**
   - `fact_extractor.py selftest` 覆盖线数、定式命名、推荐点事实。
   - 用老马提供的三张截图对应局面做合成测试（R14/Q13/P5/S14）。
   - 9x9 demo 端到端跑通；前端 `npm run build` 通过。

6. **打包**
   - 版本号 1.1.2；electron:pack → `dist-electron-v112`；git commit/push master。

---

## 四、诚实边界

- 围棋定式/棋形博大精深，本地规则库再扩充也覆盖不了所有变型和古定式；v1.1.2 的目标不是“识别所有定式”，而是**不识别的时候不要乱贴标签、不把 LLM 带偏**。
- LLM 仍可能犯错，所以保留 Verifier + 用户可关闭的 LLM 级校验开关。
- 复杂中盘战斗的方向判断仍需要 KataGo ownership / scoreLead 辅助，v1.1.2 会引入 ownership 参与阶段与安定度判断。
